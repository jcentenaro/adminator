name: Merge checks

on:
  pull_request:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        # Both supported majors. jsdom 30 needs >= 22.22.2, so 22.x must
        # resolve to a recent 22 — see "engines" in package.json.
        node-version: [22.x, 24.x]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run linting
        run: npm run lint

      - name: Run tests
        run: npm run test:run

      - name: Audit production dependencies
        run: npm audit --omit=dev --audit-level=high

      - name: Build
        run: npm run build

      - name: Get version
        id: version
        run: echo "version=v$(./ci/getVersion.sh)" >> $GITHUB_OUTPUT

      - name: Verify version
        run: ./ci/verifyVersion.sh ${{ steps.version.outputs.version }}
